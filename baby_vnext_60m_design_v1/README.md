# Baby vNext 60M design v1

This is a sealed, zero-training architecture candidate. Start with `BABY_VNEXT_ARCHITECTURE_SPEC.md`, then read the implementation report, training blueprint, and regression map.

The initialized checkpoint under `validation/` exists only to prove schema and state round-trip behavior. It records zero optimizer updates and is not a trained Baby checkpoint.

