# T6 DECISION — late-base T3 pointer (blocks 0-3 frozen)

T3: full-base pointer moved retrieval 97–102/128 but failed reversals/exact and CE>1.30.
T4: binding-only, language held, routing chance.
T5: late-base + binding + mixed loc/layout losses; language mostly held; pointer and loc stayed chance.

T5 confounds freeze vs mixed loss. T6 isolates freeze: T3 pointer + native margin (no layout, no loc CE), binding frozen, blocks 0–3 frozen, 9:1 language, Phase1G parent. If retrieval still fails, T3 needed early-block updates. If it recovers with CE≤1.30, T5's mixed layout/loc loss was the killer.
