# T5 DECISION — late-base + binding on Phase1G

T3: base-only pointer moved retrieval (97–102/128) but failed reversals/exact and CE>1.30.
T4: binding-only on frozen base updated sidecar weights (~0.013) but routing stayed chance; language held.
Neither mechanism alone is enough.

T5 tests the remaining joint: freeze blocks 0–3 (Phase1G/SF8 English scope), train later blocks + binding, 9:1 language rehearsal, T3 pointer + T2 margin + T4 localization. Parent = Phase1G U6000, not T3/T4 descendants. T3 TEST stays sealed. Hard stop if DEV CE > 1.30.
