# T7 DECISION — paired-assignment pointer on T6 scope

T6 showed late-base T3 pointer can move retrieval (one seed 105/128) with CE≤1.30; reversals still failed (best 43/64). T3 at 97–102 pointer also failed reversals (35–39/64). Independent-item SGD can fit one assignment of a pair.

T7 keeps T6 parent/scope/loss/language. The only change: each QA update is 16 complete assignment pairs (32 items, pair members adjacent so microbatches of 8 contain 4 intact pairs). Tests whether paired presentation produces reversal-complete representation without reopening TEST or lowering gates.
