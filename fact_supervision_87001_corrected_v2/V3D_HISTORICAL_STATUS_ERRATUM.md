# Additive v3d historical-status erratum

The preserved v3d artifacts remain unchanged. Their six-checkpoint measurements are
descriptive evidence on that frozen battery only. Withdraw any claim that v3d was a
fully fresh prospective transfer panel, established an English-format winner, or
identified a future parent.

The decoded audit found prompt/context reuse from v2, missing first-step EOS,
common TinyStories-loss, per-token likelihood, and format-separated diagnostics,
an execution/retry anomaly, insufficient original source/runtime provenance, and
invalid query-only accuracy/zero-margin summaries because those records had no
answer keys. Exact non-overlap cannot exclude semantic or near-duplicate exposure.

This erratum is additive and does not alter v3d, its execution artifacts, checkpoints,
or historical receipts.
