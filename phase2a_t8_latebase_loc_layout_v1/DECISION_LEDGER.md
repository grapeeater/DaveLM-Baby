# T8 DECISION — late-base loc + layout margin, no pointer

T4: binding-only on frozen base → loc chance.
T5: late-base+binding with pointer+loc+layout margin → pointer and loc both chance.
T6: late-base pointer only → retrieval moved, reversals failed, language held.
T7: T6 + paired assignments → retrieval weakened (cancel), reversals still failed.

T5 confounded pointer with loc/layout. T8 drops the T3 pointer from the loss: freeze 0–3, train late base + binding, loc CE + layout native margin, independent-item 9:1 language, Phase1G parent. Tests whether this NL layout can route when late-base is plastic and pointer is not competing.
