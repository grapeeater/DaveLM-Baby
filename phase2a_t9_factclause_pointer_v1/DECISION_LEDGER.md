# T9 DECISION — fact-clause pointer keys

T8 showed T4 name-span BindingLayout cannot route even with late-base plasticity and no competing pointer (loc 64/128 3/3). Sidecar+this-layout is a dead end at 500 updates.

T6 remains the only language-safe retrieval that moved (85/92/105) but reversals failed (best 43/64). T6 keys were name-span mean-pools, which are assignment-invariant. T7 pairing canceled that pointer.

T9 keeps T6 parent/scope/margin/9:1 independent items. Pointer keys = each name mention through the next period token (id 18). That span includes the object/relation, so swapped assignments change the keys. Tests whether T6’s reversal miss was a name-identity shortcut.
